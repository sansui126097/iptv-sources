名称: update script

on:
  push:
分支: [主页]

jobs:
构建:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v4
      
      - 名称: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - uses: pnpm/action-setup@v4
        with:
版本: 10
          run_install: false
      - 名称: Get pnpm store directory
        id: pnpm-store
        shell: bash
运行: |
            echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_OUTPUT
  
      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ steps.pnpm-store.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - run: pnpm install
      - run: pnpm build
      - run: pnpm build:static
      
      - name: Remove EPG files
        run: rm -rf ./dist/epg || true

      # - name: "Jekyll theme"
      #   run: mv ./_config.temp.yml ./m3u/_config.yml

      # - name: Deploy
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: ./m3u
      #     enable_jekyll: true
      #     cname: m3u.ibert.me

      # - name: discord
      #   uses: HerbertHe/discord-action@v1
      #   with:
      #     webhook: ${{ secrets.DISCORD_WEBHOOK }}
      #     message: ${{ github.repository }} has been successfullly updated, see https://m3u.ibert.me to get more.
